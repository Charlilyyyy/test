apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-config
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "loki-chart.labels" . | nindent 4 }}
data:
  loki.yaml: |
    {{- toYaml .Values.loki | nindent 4 }}

